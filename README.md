These scripts extracts prompt and models used (also loras with weights) from a video generated with wan video 2.2. Of course, teh data has to be there.
The bash script needs mediainfo. The python one needs pymediainfo. Data are not always put in the same places, so this is an alpha version. Feedbacks are welcome.
